<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/home"
            android:title="Home" />
        <item
            android:id="@+id/nav_search"
            android:icon="@drawable/magnify"
            android:title="Strecke suchen" />
        <item
            android:id="@+id/nav_favorites"
            android:icon="@drawable/star"
            android:title="Favoriten" />
        <item
            android:id="@+id/nav_add"
            android:icon="@drawable/plus_circle_outline"
            android:title="Strecke hinzufügen" />
    </group>

    <item android:title="Support Bereich"
        android:id="@+id/nav_extend">
        <menu>
            <item
                android:id="@+id/nav_release"
                android:visible="false"
                android:icon="@drawable/lock_open"
                android:title="Strecke freigeben" />
        </menu>
    </item>

    <item android:title="Administrator Bereich"
        android:id="@+id/nav_admin">
        <menu>
            <item
                android:id="@+id/nav_delete_user"
                android:visible="false"
                android:icon="@drawable/account_off"
                android:title="Nutzer löschen" />
            <item
                android:id="@+id/nav_delete_route"
                android:visible="false"
                android:icon="@drawable/delete"
                android:title="Strecke löschen" />
        </menu>
    </item>

</menu>
